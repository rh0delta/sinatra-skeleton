<h1><%= @song.title %></h1>

<p>
  Author: <%= @song.author %>
</p>

<div class="container">
  <p>
    <% unless @song.url.nil? %>
      <% regex = /youtube.com.*(?:\/|v=)([^&$]+)/ %>
      <% youtube_id = @song.url.match(regex)[1] %>
      <iframe width="560" height="315" src="https://www.youtube.com/embed/<%=youtube_id%>" frameborder="0" allowfullscreen></iframe>
    <% else %>
      <%= @song.url %>
    <% end %>
  </p>
</div>

<hr/>
<a href="/">Back to Tracklist</a>